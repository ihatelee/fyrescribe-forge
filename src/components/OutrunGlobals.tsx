/**
 * Placeholder for outrun-theme-specific global UI that must persist across
 * route changes. The music player is now rendered inside ManuscriptPage's
 * chapter sidebar panel, so this component is intentionally empty.
 *
 * Rendered once in App.tsx alongside GlobalSparkle, outside the route tree.
 */
const OutrunGlobals = () => null;

export default OutrunGlobals;
